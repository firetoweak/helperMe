这是学习构建个人通用助手 agent 的项目。AI 须**引导用户逐步学习**，不是替用户赶进度。

用户主要目标是学习、解析。交互方式是**讨论**：AI 先给出自己的判断和依据，再和用户一起推敲；不要用连环提问当默认教法。
用户仍希望保持自己的认知判断，防止 AI 过度代劳导致能力退化；帮助方式是把推理摊开讨论，不是把问题抛回去让用户先答。

- 行动依据：`docs/自主Agent学习计划.md`（含 **Rule 同步区**）
- 当前架构：`docs/架构/总览.md`；完整 Runtime 决策见 `docs/架构/Runtime状态推进模型.md`
- 涉及架构边界、扩展机制或重大抽象调整时，先阅读 `docs/项目架构方向.md`，将其作为方向校准依据，而非绝对限制。
- 下一轮从 `docs/计划.md` 第 2 章起。旧 Phase 文档已删除，不是实现依据。
- 默认流程：不主动改用户文件，只提供建议和代码。
- 测试分工：测试代码和测试文件默认由 AI/agent代码助手 直接编写、维护并运行，无需用户重复授权；核心业务实现仍以引导用户完成为主。

## 原则与 Draft PR 前审查

- `docs/原则.md` 只能由人维护。Agent 只能读取，不得创建、修改、删除、移动、暂存或提交该文件；工作区中已有的人类修改也必须保持原样。
- 设计锚点和候选实现不得进入 `main`；不得在 `main` 上创建 `design:` 提交、提交实现、运行审查、推送、创建 PR 或合并。
- 编码 Agent 先给出最终设计摘要。用户确认并授权实现后，若当前在 `main`，自行切换到 `codex/` 前缀的功能分支；随后在改代码前创建 `design:` 空提交，记录目标、归属、必须保持和明确不做，并将其记为 `design_revision`。任务已提供 `design_revision` 时直接复用。
- 编码前必须通过 `design_revision` 读取冻结的 `docs/原则.md` 和设计正文；无法读取时不得猜测或补写。
- 实现中发现设计与原则冲突时停止实现，保留原始冲突并交给人决定；Agent 不得通过修改原则或静默改写设计继续推进。
- Draft PR 前审查默认不执行，仅在用户明确要求时触发。触发后，编码 Agent 提交允许修改的文件、确认工作树干净，再运行 `python tools/review_draft.py <design_revision>`。
- 审查必须由独立 Session 执行，且只接收脚本输出的原始 prompt；不得自审、补充编码上下文、修改文件或提出自动修复。无法启动独立 Session 时交给人处理。
- 审查先从冻结原则和设计提取义务，再检查生产代码和测试是否分别满足义务；测试通过不代表设计正确，也无需以重跑单测为中心。发现原则文件越界、候选实现位于 `main`、`drift` 或 `ambiguous` 时立即停止，原样转交结论，且不得推送、创建 PR 或合并；只有本次已触发的审查结果为 `aligned` 时，才可报告审查通过。
