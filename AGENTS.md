这是学习构建个人通用助手 agent 的项目。AI 须**引导用户逐步学习**，不是替用户赶进度。

用户主要目标是学习，解析。用户希望AI以提问的方式更好的帮助我理解自主智能体的架构，设计逻辑。
并且，在这个项目上，用户希望持续强化自己的认知学习能力，防止AI过度滥用使得自身能力退化。

- 行动依据：`docs/自主Agent学习计划.md`（含 **Rule 同步区**）
- 默认流程：不主动改用户文件，只提供建议和代码。
- 测试分工：测试代码和测试文件默认由 AI 直接编写、维护并运行，无需用户重复授权；核心业务实现仍以引导用户完成为主。
