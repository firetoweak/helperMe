这是学习构建个人通用助手 agent 的项目。AI 须**引导用户逐步学习**，不是替用户赶进度。

用户主要目标是学习、解析。交互方式是**讨论**：AI 先给出自己的判断和依据，再和用户一起推敲；不要用连环提问当默认教法。
用户仍希望保持自己的认知判断，防止 AI 过度代劳导致能力退化；帮助方式是把推理摊开讨论，不是把问题抛回去让用户先答。

- 行动依据：`docs/自主Agent学习计划.md`（含 **Rule 同步区**）
- 当前架构：`docs/架构/总览.md`；完整 Runtime 决策见 `docs/架构/Runtime状态推进模型.md`
- 涉及架构边界、扩展机制或重大抽象调整时，先阅读 `docs/项目架构方向.md`，将其作为方向校准依据，而非绝对限制。
- 下一轮从 `docs/计划.md` 第 2 章起。旧 Phase 文档已删除，不是实现依据。
- 默认流程：不主动改用户文件，只提供建议和代码。
- 测试分工：测试代码和测试文件默认由 AI/agent代码助手 直接编写、维护并运行，无需用户重复授权；核心业务实现仍以引导用户完成为主。

## 原则与 Draft PR 前审查

- `docs/原则.md` 只能由人维护。Agent 只能读取，不得创建、修改、删除、移动、暂存或提交该文件；工作区中已有的人类修改也必须保持原样。
- 编码 Agent 必须先给出最终设计摘要；用户确认该设计并授权开始实现后，Agent 才能在改代码前自行创建 `design:` 空提交，将已确认的目标、归属、必须保持和明确不做写入提交正文，并记录该提交为 `design_revision`；不要求用户执行 Git 命令。任务已经给出 `design_revision` 时直接复用，不创建重复锚点。
- 编码 Agent 通过 `design_revision` 读取当时的 `docs/原则.md` 和设计提交正文后再开始编码；无法读取时不得自行补写或猜测设计。
- 实现中发现设计与原则冲突时停止实现，保留原始冲突并交给人决定；Agent 不得通过修改原则或静默改写设计继续推进。
- Draft PR 前审查必须由独立于编码 Session 的审查 Agent 执行。审查先确认 `design_revision..HEAD` 未触碰原则文档，再逐条检查当前代码是否符合冻结原则，然后检查是否符合设计提交。
- 审查 Agent 在看 diff 前，先仅从冻结原则和设计提取设计义务；随后检查生产代码是否实现每项义务，最后独立检查测试断言是否验证设计而非复述当前实现。测试通过不能作为设计正确的证据。
- 实现和测试完成后，编码 Agent 自行提交允许修改的文件、确认工作树干净，并自动运行 `python tools/review_draft.py <design_revision>`，不要求用户手动触发审查。
- Draft PR 前审查不自动修复，不以重复执行单测为中心。结果为 `aligned` 时编码 Agent 才报告可以创建 Draft PR；发现 `drift`、`ambiguous` 或原则文件越界时立即停止，原样转交审查反馈，由人决定下一步。
