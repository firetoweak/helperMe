---
description: 改 core/tools/docs 时的门禁——单层改动、分工清晰、benchmark 验收
globs: core/**,tools/**,docs/**
alwaysApply: false
---

# Agent 代码改动门禁

## 改代码前

1. 读 `docs/自主Agent学习计划.md` 第四节 + **Rule 同步区**
2. 声明：对应哪项任务、解决什么**可观察**问题
3. 若单次对话会动 ≥2 个层级，必须先让用户选层：
   - A system prompt（`core/agent.py`）
   - B tool handler / docstring（`tools/**`）
   - C tools_executor 兜底（`core/tools_executor.py`）
   - D 学习计划文档

## 四层分工（不可混写）

| 层 | 放什么 | 不放什么 |
|----|--------|----------|
| system prompt | 通用行为策略 | 工具参数细节、项目路径 |
| tool docstring | 何时用、失败怎么调整 | 完整返回字段列表 |
| Field(description) | 参数语义 | 行为策略 |
| tools_executor | 统一协议兜底 | 业务逻辑 |

## 阶段约束

- 「Rule 同步区」标 **禁止** 的事项，不得擅自做
- 未跑通 benchmark（patch → get_changes → 总结与 diff 一致）前，不推进效率优化、多轮 REPL 等后置项

## 涉及改文件的验收

改完后提示用户跑 benchmark，检查 trace 是否出现 **get_changes**，总结是否与 **diff** 一致；不宣布「已稳定」。

## 反例

❌ 一次 PR 式改动：executor 规范化 + prompt 重写 + 6 个 docstring  
✅ 用户选一层 → 改一层 → run.log 验收 → 再下一层
